/**
 * Radar chart — placeholder for future implementation.
 */

export function renderRadar(containerId) {
  const el = document.getElementById(containerId);
  if (!el) return;
  el.textContent = 'Radar chart: coming soon';
  el.style.cssText = 'color:var(--text-t);font-size:12px;text-align:center;padding:40px;';
}
