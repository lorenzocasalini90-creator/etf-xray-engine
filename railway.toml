[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONPATH=/app:$PYTHONPATH uvicorn api.main:app --host 0.0.0.0 --port $PORT --workers 2 --timeout-keep-alive 60"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
